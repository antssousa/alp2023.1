let salarioMensalAtual = Number(prompt("Digite o salário mensal atual do funcionário:"));

let percentualDeReajuste = Number(prompt("Digite o percentual de reajuste (apenas números, sem o símbolo '%'):"));

let valorDoReajuste = salarioMensalAtual * (percentualDeReajuste / 100);

let novoSalario = salarioMensalAtual + valorDoReajuste;

console.log(`O valor do novo salário é: R$${novoSalario.toFixed(2)}`);


let salarioMensalAtual = prompt("Digite o salário mensal atual do funcionário:");

let percentualDeReajuste = prompt("Digite o percentual de reajuste (apenas números, sem o símbolo '%'):");

let valorDoReajuste = salarioMensalAtual * (percentualDeReajuste / 100);

const novoSalario = salarioMensalAtual + valorDoReajuste;

console.log(`O valor do novo salário é: R$${novoSalario.toFixed(2)}`);

let x = Number(prompt());
let y = Number(prompt());
let z = Number(prompt());

let w = x * 365 + y * 30 + z;

console.log(`A saída é: ${w}`);

let _____ = Number(prompt("Digite a base do retângulo:"));
let _____ = Number(prompt("Digite a altura do retângulo:"));

let _____ = _____ * _____;

console.log(`A área do retângulo é: ${_____}`);


let nota1 = Number(prompt("Digite a primeira nota do aluno:")); 
let nota2 = Number(prompt("Digite a segunda nota do aluno:")); 
let nota3 = Number(prompt("Digite a terceira nota do aluno:")); 
let mediaFinal = (nota1 + nota2 + nota3) / 3; 
console.log(`A média final do aluno é: ${mediaFinal.toFixed(2)}`);

let valor = Number(prompt("Digite um valor:"));
_______________________________________

console.log(`O antecessor do valor inserido é: ${antecessor}`);


// Solicita o número total de eleitores

// Solicita o número de votos em branco

// Solicita o número de votos nulos

// Solicita o número de votos válidos

// Calcula o percentual de votos em branco

// Calcula o percentual de votos nulos

// Calcula o percentual de votos válidos

// Exibe o percentual de votos em branco no console

// Exibe o percentual de votos nulos no console

// Exibe o percentual de votos válidos no console





